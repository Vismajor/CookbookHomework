<p id="notice"><%= notice %></p>

<p>
  <strong>Catgeroy:</strong>
  <%= @category.name %>
</p>

<p>
  <strong>Recipes:</strong>
  <% @category.recipes.each do |recipe| %>
  <%= "#{recipe.name}" %>
  <% end %>
</p>

<%= link_to 'Edit', edit_category_path(@category) %> |
<%= link_to 'Back', categories_path %>
